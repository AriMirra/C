//
// Created by arimi on 16-Mar-17.
//

#include <stdint.h>
#include <afxres.h>
#include <stdio.h>
#include "DinamicMemory.h"

/*
int main(){

    struct nodo *invertirLista (struct nodo *cabecera)
    {
        int temp;
        struct nodo *p, *q, *r;
        for (q = cabecera; q-> siguiente != NULL; q = q-> siguiente);
        for (p = cabecera; p != q && q-> siguiente != p; p= p-> siguiente)
        {
            //cambia el p actual con el último elemento
            temp = p-> info;
            p -> info = q -> info;
            q-> info = temp;
            for (r=p; r-> siguiente != q; r = r-> siguiente);
            q = r;
        }
        return cabecera;
    }
    return 0;
}
*/
